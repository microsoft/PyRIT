# Pin npm registry to the public npmjs registry.
# Required by Microsoft Secure Supply Chain (CFS0001) policy — every
# package.json must have a sibling .npmrc that explicitly sets the registry.
registry=https://registry.npmjs.org/
